import { PreferenceModel } from './preference.model';

describe('PreferenceModel', () => {
  it('should create an instance', () => {
    // @ts-ignore
    expect(new PreferenceModel()).toBeTruthy();
  });
});
