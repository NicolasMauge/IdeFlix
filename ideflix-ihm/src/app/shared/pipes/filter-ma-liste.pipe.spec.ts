import { FilterMaListePipe } from './filter-ma-liste.pipe';

describe('FilterMaListePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterMaListePipe();
    expect(pipe).toBeTruthy();
  });
});
