import { ValuesFromKeyPipe } from './values-from-key.pipe';

describe('ValuesFromKeyPipe', () => {
  it('create an instance', () => {
    const pipe = new ValuesFromKeyPipe();
    expect(pipe).toBeTruthy();
  });
});
