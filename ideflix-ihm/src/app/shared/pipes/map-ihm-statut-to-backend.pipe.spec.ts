import { MapIhmStatutToBackendPipe } from './map-ihm-statut-to-backend.pipe';

describe('MapIhmStatutToBackendPipe', () => {
  it('create an instance', () => {
    const pipe = new MapIhmStatutToBackendPipe();
    expect(pipe).toBeTruthy();
  });
});
