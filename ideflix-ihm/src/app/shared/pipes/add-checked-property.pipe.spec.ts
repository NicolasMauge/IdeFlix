import { AddCheckedPropertyPipe } from './add-checked-property.pipe';

describe('AddCheckedPropertyPipe', () => {
  it('create an instance', () => {
    const pipe = new AddCheckedPropertyPipe();
    expect(pipe).toBeTruthy();
  });
});
