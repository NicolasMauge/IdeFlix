import { MediaMaListeModel } from './media-ma-liste.model';

describe('MediaMaListeModel', () => {
  it('should create an instance', () => {
    expect(new MediaMaListeModel()).toBeTruthy();
  });
});
