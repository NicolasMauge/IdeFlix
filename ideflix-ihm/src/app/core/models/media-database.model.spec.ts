import { MediaDatabaseModel } from './media-database.model';

describe('MediaDatabaseModel', () => {
  it('should create an instance', () => {
    expect(new MediaDatabaseModel()).toBeTruthy();
  });
});
